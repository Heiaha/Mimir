model:
  L1: 512
  N_BUCKETS: 8

scaling:
  lambda_: 0.8
  nnue_2_score: 400
  cp_scaling: 157.41

training:
  batch_size: 8192
  workers: 8
  device: cuda
  training_dir: training
  testing_dir: testing
  save_dir: nets
  epochs: 30
  max_lr: 1.0e-3
  warmup_frac: 0.05

#load: nets/2026-06-28T03-52-28/model_checkpoint_3.pth
